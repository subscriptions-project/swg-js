comment: false
coverage:
  status:
    project:
      default:
        base: auto
        target: auto
        threshold: 0.05%
    patch:
      default:
        threshold: 1%
        if_not_found: success
